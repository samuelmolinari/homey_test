<%= turbo_frame_tag 'project_users' do %>
    <% if notice.present? %>
      <div class="w-full">
      <p class="inline-block px-3 py-2 mb-5 font-medium text-green-500 rounded-lg bg-green-50" id="notice"><%= notice %></p>
    <% end %>

    <% content_for :title, "Project users" %>

    <div class="flex items-center justify-between">
      <h1 class="text-4xl font-bold">Project users</h1>
      <%= link_to "New project user", new_project_user_path(@project), class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium", data: { turbo_frame: '_top' } %>
    </div>

    <div id="project_users" class="min-w-full">
      <% @project_users.each do |project_user| %>
        <%= render project_user %>
        <p>
          <%= link_to "Remove", project_user_path(@project, project_user), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium", data: { turbo_method: :delete, turbo_confirm: 'Are you sure?', turbo_frame: '_top' } %>
        </p>
      <% end %>
    </div>
  </div>
<% end %>
